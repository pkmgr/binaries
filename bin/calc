#!/usr/bin/env bash

APPNAME="$(basename $0)"
USER="${SUDO_USER:-${USER}}"
shopt -u extglob direxpand globstar

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
##@Version     : 010920210727-git
# @Author      : Jason
# @Contact     : casjaysdev@casjay.net
# @File        : calc
# @Created     : Wed, Aug 05, 2020, 02:00 EST
# @License     : WTFPL
# @Copyright   : Copyright (c) CasjaysDev
# @Description : wrapper for qalc/bc
#
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# Set functions

SCRIPTSFUNCTURL="${SCRIPTSAPPFUNCTURL:-https://github.com/dfmgr/installer/raw/master/functions}"
SCRIPTSFUNCTDIR="${SCRIPTSAPPFUNCTDIR:-/usr/local/share/CasjaysDev/scripts}"
SCRIPTSFUNCTFILE="${SCRIPTSAPPFUNCTFILE:-testing.bash}"

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

if [ -f "$PWD/functions/$SCRIPTSFUNCTFILE" ]; then
  . "$PWD/functions/$SCRIPTSFUNCTFILE"
elif [ -f "$SCRIPTSFUNCTDIR/functions/$SCRIPTSFUNCTFILE" ]; then
  . "$SCRIPTSFUNCTDIR/functions/$SCRIPTSFUNCTFILE"
fi

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
systemmgr_install
__options "$@"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

__usage() {
  printf_cyan 'Usage: "20 * 4/2 + 23 - 14"'
  printf_red "press q or ctrl + c to exit"
}

[ -z "$1" ] && __help
if [ $# -ne 1 ]; then
  printf_info 'You must quote your params IE: calc "2 + 4" or "2*2"'
  exit 1
fi

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

INPUT="$*"
OUTPUT="$(echo "$INPUT" | sed '/^$/d;s/[[:blank:]]//g')"

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

if cmd_exists qalc; then
  calc_app() { qalc -c "$OUTPUT"; }
elif cmd_exists bc; then
  calc_app() { echo "$INPUT = $(echo "$OUTPUT" | bc -q)"; }
else
  printf_exit "Can't find a calculator app!"
fi

if [ "$1" = "--term" ]; then
  clear
  echo -e "\n\n"
  __usage
  while :; do
    printf_custom_question "2" "What is it you would like to solve?"
    printf_answer "INPUT" "200"
    if [ "$INPUT" = "q" ]; then
      break
    else
      clear
      echo -e "\n\n"
      if [ "$INPUT" = "h" ] || [ "$INPUT" = "?" ]; then
        __usage
      else
        history -s "$INPUT"
        OUTPUT="$(echo "$INPUT" | sed '/^$/d;s/[[:blank:]]//g')"
        printf_custom "5" "Your answer is: $(calc_app "$OUTPUT")"
      fi
      echo -e "\n"
    fi
  done
  echo ""
else
  printf_green "$(calc_app)"
fi

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# end
