#!/usr/bin/env bash

APPNAME="$(basename $0)"
USER="${SUDO_USER:-${USER}}"

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
##@Version     : 010920210727-git
# @Author      : Jason
# @Contact     : casjaysdev@casjay.net
# @File        : run_in_term
# @Created     : Wed, Aug 05, 2020, 02:00 EST
# @License     : WTFPL
# @Copyright   : Copyright (c) CasjaysDev
# @Description : Keep a terminal app running
#
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# Set functions

SCRIPTSFUNCTURL="${SCRIPTSAPPFUNCTURL:-https://github.com/dfmgr/installer/raw/master/functions}"
SCRIPTSFUNCTDIR="${SCRIPTSAPPFUNCTDIR:-/usr/local/share/CasjaysDev/scripts}"
SCRIPTSFUNCTFILE="${SCRIPTSAPPFUNCTFILE:-testing.bash}"

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

if [ -f "$PWD/functions/$SCRIPTSFUNCTFILE" ]; then
  . "$PWD/functions/$SCRIPTSFUNCTFILE"
elif [ -f "$SCRIPTSFUNCTDIR/functions/$SCRIPTSFUNCTFILE" ]; then
  . "$SCRIPTSFUNCTDIR/functions/$SCRIPTSFUNCTFILE"
fi

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
dfmgr_install "$@"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

run_in_term() {
  local CMD="$1"
  shift 1
  if [ $# -gt 0 ]; then
    $CMD "$@"
    exit $?
  else
    echo -e "\n\n\n"
    printf_red "Type exit or ctrl + c to exit"
    printf_cyan "Usage: Enter the commands for $CMD"
    printf_cyan "To show this message again type: _help_"
    while :; do
      printf_read_question "5" "What would you like to pass to $CMD?" "__ANSWER" "120"
      if [ "$__ANSWER" = exit ] || [ "$__ANSWER" = quit ] || [ "$__ANSWER" = !q ]; then clear && break; fi
      if [ "$__ANSWER" = _help_ ]; then
        clear
        printf_cyan "Usage: Enter the commands for $CMD or type exit to quit"
        continue
      fi
      clear
      echo -e "\n\n\n"
      $CMD "$__ANSWER" 2>/dev/null | printf_readline
      echo -e "\n"
    done
  fi
}

run_in_term "$@"

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# end
