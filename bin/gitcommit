#!/usr/bin/env bash

APPNAME="$(basename $0)"
USER="${SUDO_USER:-${USER}}"
HOME="${USER_HOME:-${HOME}}"

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
##@Version     : 010920210727-git
# @Author      : Jason
# @Contact     : casjaysdev@casjay.net
# @File        : gitcommit
# @Created     : Wed, Aug 05, 2020, 02:00 EST
# @License     : WTFPL
# @Copyright   : Copyright (c) CasjaysDev
# @Description : commit changes to current git repo
#
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# Set functions

SCRIPTSFUNCTURL="${SCRIPTSAPPFUNCTURL:-https://github.com/dfmgr/installer/raw/master/functions}"
SCRIPTSFUNCTDIR="${SCRIPTSAPPFUNCTDIR:-/usr/local/share/CasjaysDev/scripts}"
SCRIPTSFUNCTFILE="${SCRIPTSAPPFUNCTFILE:-testing.bash}"

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

if [ -f "$PWD/functions/$SCRIPTSFUNCTFILE" ]; then
  . "$PWD/functions/$SCRIPTSFUNCTFILE"
elif [ -f "$SCRIPTSFUNCTDIR/functions/$SCRIPTSFUNCTFILE" ]; then
  . "$SCRIPTSFUNCTDIR/functions/$SCRIPTSFUNCTFILE"
fi

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# check for needed applications

__check_app git

if [ "$(git rev-parse --is-inside-work-tree 2>/dev/null)" == "" ]; then
  printf_exit "You are not in a git repo"
fi

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Main application

if [ "$1" = "-m" ] || [ "$1" = "--m" ] || [ "$1" = "--mess" ] || [ "$1" = "--message" ]; then
  MESSAGE="$1"
  if [ -n "$2" ]; then
    newmess="$2"
  fi
else
  MESSAGE="$1"
fi

case "$MESSAGE" in

added)
  shift 1
  ARGS="$*"
  mess=" 📦➕ Added: $ARGS 📦➕ "
  ;;

new)
  shift 1
  ARGS="$*"
  mess=" 🆕📦 NEW: $ARGS 📦🆕 "
  ;;

improve)
  shift 1
  ARGS="$*"
  mess=" 👌 IMPROVE: $ARGS 👌 "
  ;;

fixed)
  shift 1
  ARGS="$*"
  mess=" 🐛 FIXED: $ARGS 🐛 "
  ;;

release)
  shift 1
  ARGS="$*"
  mess=" 🚀 RELEASE: $ARGS 🚀 "
  ;;

docs)
  shift 1
  ARGS="$*"
  mess=" 📖 DOCS: $ARGS 📖 "
  ;;

test)
  shift 1
  ARGS="$*"
  mess=" 🤖 TEST: $ARGS 🤖 "
  ;;

breaking)
  shift 1
  ARGS="$*"
  mess=" ⚒️ BREAKING: $ARGS ⚒️ "
  ;;

refactor)
  shift 1
  mess=" ✔️ 〰️ Refactor Code 〰️ ✔️  "
  ;;

fixes)
  shift 1
  mess=" 🏠🐜❗ Minor Fixes ❗🐜🏠 "
  ;;

permission | permissions)
  shift 1
  mess=" 🏠🔥❗ Fixed Permissions ❗🔥🏠 "
  ;;

bug | bugs)
  shift 1
  mess=" 🐛🦈🏠❗ Bug Fixes ❗🏠🦈🐛 "
  ;;

alot)
  shift 1
  mess=" 🦈🏠🐜❗ Fixes and Updates ❗🐜🦈🏠 "
  ;;

docker)
  shift 1
  mess=" 🐜❇🦈🏠❗ Added Docker Workflow ❗🏠🦈❇🐜 "
  ;;

node)
  shift 1
  mess=" 🐜❇🦈🏠❗ Added nodejs Workflow ❗🏠🦈❇🐜 "
  ;;

ruby)
  shift 1
  mess=" 🐜❇🦈🏠❗ Added ruby Workflow ❗🏠🦈❇🐜 "
  ;;

php)
  shift 1
  mess=" 🐜❇🦈🏠❗ Added php Workflow ❗🏠🦈❇🐜 "
  ;;

perl)
  shift 1
  mess=" 🐜❇🦈🏠❗ Added perl Workflow ❗🏠🦈❇🐜 "
  ;;

python)
  shift 1
  mess=" 🐜❇🦈🏠❗ Added python Workflow ❗🏠🦈❇🐜 "
  ;;

scratchpad)
  shift 1
  mess=" 🐜📓❇📓 Modified my scratchpad 📓❇📓🐜 "
  ;;

todo)
  shift 1
  mess=" 🐜📓❇📓 Modified my todo list 📓❇📓🐜 "
  ;;

notes)
  shift 1
  mess=" 🐜📓❇📓 Modified my notes 📓❇📓🐜 "
  ;;

init)
  shift 1
  mess=" 🦈🏠🐜❗ Initial Commit 📍🐜🦈🏠 "
  ;;

random)
  shift 1
  mess="🐜:arrow_right: Random: $(curl -s https://commitment.herokuapp.com/index.txt) :arrow_left:🐜"
  ;;

custom)
  shift 1
  if [ -n "$1" ]; then
    mess="$1"
  else
    printf_yellow "You must specify your commit message"
  fi
  ;;

setup)
  shift 1
  gitadmin setup
  exit
  ;;

*)
  [ ! "$(echo $* | wc -m)" -lt "5" ] || printf_exit "Message must be longer than 5 characters"
  if [ -n "$1" ]; then
    mess="$newmess"
  else
    shift 1
    mess=" 🦈🏠❗ Major Fixes ❗🏠🦈 "
  fi
  ;;

esac

oldpwd="$(pwd)"
gitdir="$(__git_top_dir)"
repo="$(__git_fetch_remote)"

[ ! -z "$repo" ] || repo="localdir"

if [ ! -d "$gitdir" ]; then
  printf_exit "$gitdir doesn't seem to be a git repo"
else
  __cd "$gitdir"
fi

if __git_porcelain; then
  printf_red "There are no changes to commit"
else
  printf_info "Commiting Changes with message: $mess"

  date +"%m%d%Y%H%M-git" >version.txt
  if [ -f gitmasterconfig ] && [ -d .git ]; then
    cp -Rf gitmasterconfig .git/config
  fi

  git add . >/dev/null 2>&1

  if [ -z "$mess" ]; then
    git commit -q -S -m " 🦈🏠🐜 Fixes and Updates 🐜🦈🏠 " 2>/dev/null
  else
    git commit -q -S -m "$mess" 2>/dev/null
  fi
  __getexitcode "Successfully committed the changes" "Failed to commit the changes"

  if __am_i_online && [ -z "$push" ]; then
    if [ "$repo" != "localdir" ]; then
      if [[ "$*" =~ --automated ]]; then
        printf_green "Pushing the changes to $repo"
        git push -q
        __getexitcode "Successfully pushed the changes for commit ID: $(git rev-parse --short HEAD)" "Failed to push the changes"
      else
        printf_question "Should I push to $repo? [y/n]"
        read -r -n 1 -s push && echo
        if [[ $push == "y" || $push == "Y" ]]; then
          printf_green "Pushing the changes to $repo"
          git push -q
          __getexitcode "Successfully pushed the changes for commit ID: $(git rev-parse --short HEAD)" "Failed to push the changes"
        fi
      fi
    fi
  else
    printf_green "commit ID: $(git rev-parse --short HEAD)"
    printf_red "No internet connection. Skipping push"
  fi

  __cd "$oldpwd"
fi

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
exit $?
# end
